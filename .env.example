NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# Used to sign auth cookies and encrypt sensitive cookie data.
# Generate one with: openssl rand -base64 32
SESSION_SECRET=replace-with-32-bytes-of-random-base64

# First admin (created on `npm run seed`)
ADMIN_EMAIL=yehiel@ykl.asia
ADMIN_PASSWORD=ChangeMe2026!

# Public app URL (no trailing slash). Set to your Vercel URL after first deploy.
APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_URL=http://localhost:3000
